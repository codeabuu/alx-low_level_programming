#include "1-pi.h"
#include "1-pi.h"
#include <stdio.h>

/**
 * main - checks the whole code
 *
 * Return: is always 0.
 */
int main(void)
{
	float a;
	float r;

	r = 98;
	a = PI * r * r;
	printf("%.3f\n", a);
	return (0);
}
