#include "main.h"
/**
 * _abs - absolute value of an integer
 *
 * @n: the int to be checked
 * int - integer
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
return (-n);
}

